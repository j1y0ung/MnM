<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.mnm.dao.mybatis.mapper.AccountMapper">

	<insert id="insertAccount" parameterType="Account">
		insert into account
		(userid, email, name, addr, phone, favcategory, pwd)
    	values
		(#{userid}, #{email}, #{name}, #{addr}, #{phone}, #{favcategory}, #{pwd})
	</insert>
	
	<delete id="deleteAccount" parameterType="String">
		delete from account
		where userid=#{userid}
	</delete>
	
	<update id="updateAccount" parameterType="Account">
		udpate account
		set email=#{email}, addr=#{addr}, phone=#{phone}, favcategory=#{favcategory}, pwd=#{pwd}
		where userid=#{userid}
	</update>
	
	<select id="getPwd" parameterType="String" resultType="String">
		select pwd from account
		where userid=#{id}
	</select>
	
	<select id="getAccount" parameterType="String" resultType="Account">
		select * from account
		where userid=#{id}
	</select>

</mapper>
